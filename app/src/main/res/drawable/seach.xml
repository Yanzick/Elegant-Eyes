<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Màu nền của thanh SearchView -->
    <stroke
        android:width="5dp"
        android:color="@color/blue" /> <!-- Viền màu xám -->
    <corners android:radius="30dp" /> <!-- Độ cong của góc, ở đây là 20dp -->
</shape>

